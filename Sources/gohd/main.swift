// gohd — daemon. Runs under launchd as a LaunchAgent. Owns the network, the
// job queue, and the disk.
//
// Bootstrap stub: prints the module name. Real daemon logic lands in a later
// slice.

print("gohd")
