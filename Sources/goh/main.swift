// goh — CLI client. Thin. Talks to the daemon (`gohd`) over XPC and exits fast.
//
// Bootstrap stub: prints the module name. Real command handling — and the
// `goh`/`gohd` XPC contract — lands in the next slice.

print("goh")
